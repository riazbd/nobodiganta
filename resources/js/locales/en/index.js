import { common } from './common';
import { pages } from './pages';
import { widgets } from './widgets';
import { admin } from './admin';

export const en = {
  ...common,
  ...pages,
  ...widgets,
  ...admin,
};
